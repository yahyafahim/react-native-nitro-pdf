///
/// HybridNitroPdfComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroPdfComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroPdfSpecSwift.hpp"
#import "NitroPdf-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitropdf;
using namespace margelo::nitro::nitropdf::views;

/**
 * Represents the React Native View holder for the Nitro "NitroPdf" HybridView.
 */
@interface HybridNitroPdfComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroPdfComponent {
  std::shared_ptr<HybridNitroPdfSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroPdfComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroPdfComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridNitroPdfSpec> hybridView = NitroPdf::NitroPdfAutolinking::createNitroPdf();
    _hybridView = std::dynamic_pointer_cast<HybridNitroPdfSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroPdf::HybridNitroPdfSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridNitroPdfProps const>(props);
  auto& newViewProps = const_cast<HybridNitroPdfProps&>(newViewPropsConst);
  NitroPdf::HybridNitroPdfSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // source: string
  if (newViewProps.source.isDirty) {
    swiftPart.setSource(newViewProps.source.value);
    newViewProps.source.isDirty = false;
  }
  // fitPolicy: enum
  if (newViewProps.fitPolicy.isDirty) {
    swiftPart.setFitPolicy(static_cast<int>(newViewProps.fitPolicy.value));
    newViewProps.fitPolicy.isDirty = false;
  }
  // backgroundColor: optional
  if (newViewProps.backgroundColor.isDirty) {
    swiftPart.setBackgroundColor(newViewProps.backgroundColor.value);
    newViewProps.backgroundColor.isDirty = false;
  }
  // onLoadSuccess: optional
  if (newViewProps.onLoadSuccess.isDirty) {
    swiftPart.setOnLoadSuccess(newViewProps.onLoadSuccess.value);
    newViewProps.onLoadSuccess.isDirty = false;
  }
  // onLoadError: optional
  if (newViewProps.onLoadError.isDirty) {
    swiftPart.setOnLoadError(newViewProps.onLoadError.value);
    newViewProps.onLoadError.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroPdf::NitroPdfAutolinking::isNitroPdfRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroPdf::HybridNitroPdfSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroPdf::HybridNitroPdfSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
