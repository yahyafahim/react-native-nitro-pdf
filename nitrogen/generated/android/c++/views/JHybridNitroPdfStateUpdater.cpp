///
/// JHybridNitroPdfStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridNitroPdfStateUpdater.hpp"
#include "views/HybridNitroPdfComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitropdf::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroPdfState>;

void JHybridNitroPdfStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroPdfSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridNitroPdfSpec> hybridView = javaView->getJHybridNitroPdfSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroPdfState& data = concreteState->getData();
  const std::shared_ptr<HybridNitroPdfProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridNitroPdfState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->source.isDirty) {
    hybridView->setSource(props->source.value);
    props->source.isDirty = false;
  }
  if (props->fitPolicy.isDirty) {
    hybridView->setFitPolicy(props->fitPolicy.value);
    props->fitPolicy.isDirty = false;
  }
  if (props->backgroundColor.isDirty) {
    hybridView->setBackgroundColor(props->backgroundColor.value);
    props->backgroundColor.isDirty = false;
  }
  if (props->onLoadSuccess.isDirty) {
    hybridView->setOnLoadSuccess(props->onLoadSuccess.value);
    props->onLoadSuccess.isDirty = false;
  }
  if (props->onLoadError.isDirty) {
    hybridView->setOnLoadError(props->onLoadError.value);
    props->onLoadError.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::nitropdf::views
