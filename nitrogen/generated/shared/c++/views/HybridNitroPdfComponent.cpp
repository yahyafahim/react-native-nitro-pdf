///
/// HybridNitroPdfComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridNitroPdfComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitropdf::views {

  extern const char HybridNitroPdfComponentName[] = "NitroPdf";

  HybridNitroPdfProps::HybridNitroPdfProps(const react::PropsParserContext& context,
                                           const HybridNitroPdfProps& sourceProps,
                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    source([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("source", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.source;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.source);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.source: ") + exc.what());
      }
    }()),
    fitPolicy([&]() -> CachedProp<FitPolicy> {
      try {
        const react::RawValue* rawValue = rawProps.at("fitPolicy", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.fitPolicy;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<FitPolicy>::fromRawValue(*runtime, value, sourceProps.fitPolicy);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.fitPolicy: ") + exc.what());
      }
    }()),
    backgroundColor([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("backgroundColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.backgroundColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.backgroundColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.backgroundColor: ") + exc.what());
      }
    }()),
    onLoadSuccess([&]() -> CachedProp<std::optional<std::function<void(const PdfMetadata& /* metadata */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onLoadSuccess", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onLoadSuccess;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const PdfMetadata& /* metadata */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onLoadSuccess);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.onLoadSuccess: ") + exc.what());
      }
    }()),
    onLoadError([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* message */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onLoadError", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onLoadError;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* message */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onLoadError);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.onLoadError: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroPdfSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroPdfSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroPdf.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridNitroPdfProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("source"): return true;
      case hashString("fitPolicy"): return true;
      case hashString("backgroundColor"): return true;
      case hashString("onLoadSuccess"): return true;
      case hashString("onLoadError"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroPdfComponentDescriptor::HybridNitroPdfComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroPdfComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                    const std::shared_ptr<const react::Props>& props,
                                                                                    react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroPdfShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroPdfComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridNitroPdfShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridNitroPdfProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridNitroPdfProps>& props = std::const_pointer_cast<HybridNitroPdfProps>(constProps);
    HybridNitroPdfState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitropdf::views
